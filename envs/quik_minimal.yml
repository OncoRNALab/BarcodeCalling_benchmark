name: quik_minimal
channels:
  - conda-forge
  - defaults

dependencies:
  # Build tools (lightweight)
  - cmake>=3.22
  - make
  
  # Python for scripts  
  - python=3.11
  - numpy
  - pandas
  - biopython

# NOTE: CUDA is loaded from VSC HPC system modules via beforeScript:
# module load CUDA/12.6.0
# module load CMake/3.29.3-GCCcore-13.3.0

