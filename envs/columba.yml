name: columba
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - cmake>=3.20
  - cxx-compiler  # Provides g++, gcc for building Columba
  - make
  - git
  - sed
  - grep
  - coreutils
  - biopython
  - numpy
